/// Template for `api_keys.dart`. Copy this file to `api_keys.dart` (which is
/// gitignored) and fill in your real keys. Committing keys to git exposes them.
class ApiKeys {
  ApiKeys._();

  /// Google Safe Browsing v4 API key. Empty string disables the source.
  static const String googleSafeBrowsing = '';

  /// VirusTotal API key. Empty string keeps the source mocked.
  static const String virusTotal = '';
}
